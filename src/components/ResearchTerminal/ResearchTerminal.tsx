import{useState}from'react';import{useStore}from'../../store/useStore';import{generateReport}from'../../services/api';const REPORTS=[{cat:'Investment Memos',items:[{label:'NVIDIA (NVDA)',prompt:'Generate Investment Memo for NVIDIA.'},{label:'Ecobank (ETI)',prompt:'Generate Investment Memo for Ecobank.'}]},{cat:'Market Research',items:[{label:'African Fintech',prompt:'Generate African Fintech Report.'},{label:'Gold Thesis',prompt:'Generate Gold Market Thesis.'}]},{cat:'Client Reports',items:[{label:'Competitive Intel',prompt:'Generate Competitive Intelligence Report.'},{label:'Market Entry',prompt:'Generate Market Entry Analysis.'}]}];export function ResearchTerminal(){const{addMessage,updateLastMessage}=useStore();const[gen,setGen]=useState(false);const handle=async(label:string,prompt:string)=>{if(gen)return;setGen(true);addMessage({id:Math.random().toString(36),role:'user',content:prompt});addMessage({id:Math.random().toString(36),role:'assistant',content:''});try{const r=await generateReport(prompt,label);if(r.content){const msgs=[...useStore.getState().messages];msgs[msgs.length-1]={...msgs[msgs.length-1],content:r.content};useStore.setState({messages:msgs})}}catch(e){updateLastMessage('Report generation failed.')};setGen(false)};return(<div className='terminal-view'><h2>Research Terminal</h2><p>Generate professional reports in under 60 seconds</p><div className='report-grid'>{REPORTS.map(cat=>(<div key={cat.cat}><h3>{cat.cat}</h3>{cat.items.map(item=>(<button key={item.label} onClick={()=>handle(item.label,item.prompt)} disabled={gen}>{item.label}</button>))}</div>))}</div></div>)}
